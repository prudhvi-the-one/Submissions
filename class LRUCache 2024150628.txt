class LRUCache{
    int capacity;
    int cache[capacity]=capacity*{0};
    int used[capacity]=capacity*{0};
    int get(int value){
            for(int i=0;i<capacity;i++){
                if(cache[i]==value){
                    used[i]++;
                     return cache[key];
                     
                }
                else{
                    return -1;
                }
            }
          
    }
    int put(int key,int value){
        if(key<capacity){
            if(cache[key]==0){
                cache[key]=value;
            }
            else{
                int min_used=0;
                for(int i=0;i<capacity;i++){
                    if(used[i]<min){
                        min_used=used[i];
                    }
                }
                for(int i=0;i<capacity;i++){
                    if(used[i]==min){
                        cache[i]=value;
                    }
                }
            }
            
        }
        
    }
}